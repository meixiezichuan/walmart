spring.application.name=walmart-wallet-service

server.port=${WALLET_SERVICE_PORT}

spring.datasource.driver-class-name=${mysql.driver-class-name}
spring.datasource.url=${mysql.url}
spring.datasource.username=${mysql.username}
spring.datasource.password=${mysql.password}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=com.walmart_6g.walmartWalletService.config.MysqlConfig

